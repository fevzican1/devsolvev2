# Cloudflare Pages Configuration
name = "devsolvev2"
compatibility_date = "2026-04-18"
pages_build_output_dir = "out"

# Functions configuration
[build]
command = "npm run build"

# Ensure the functions directory is picked up
# Functions in /functions/ are automatically deployed with Cloudflare Pages
