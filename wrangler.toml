# Cloudflare Pages Configuration
name = "devsolvev2"
compatibility_date = "2026-04-18"
pages_build_output_dir = "out"

# Static assets are served directly by Pages. `functions/[[path]].ts` adds
# deterministic, dependency-free edge handling only for /k/* and sitemaps.
